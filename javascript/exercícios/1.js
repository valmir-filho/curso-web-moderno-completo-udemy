// Crie uma função que dado dois valores (passados como parâmetros) mostre no console a soma, a subtração, a multiplicação e a divisão desses valores.
function calculadora(x, y) {
    console.log(x + y, x - y, x * y, x / y);
}
calculadora(8, 4);
