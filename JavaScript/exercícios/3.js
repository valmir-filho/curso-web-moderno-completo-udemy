// Crie uma função que receba dois parâmetros (base e expoente) e retorne a base elevada ao expoente.
function potencia1(base, expoente) {
    console.log(Math.pow(2, 3));
}
potencia1();
function potencia2(base, expoente) {
    console.log(base ** expoente);  // Outra forma para realização do cálculo da potência.
}
potencia2(2, 3);